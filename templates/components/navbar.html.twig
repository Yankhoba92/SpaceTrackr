<nav class="navbar navbar-expand-lg bg-light">
    <ul class="navbar-nav justify-content-between w-50 m-auto">
        <div class="d-flex">
            <li class="nav-item">
                <a class="nav-link fs-5 fw-bold text-dark" aria-current="page" href="{{ path('app_home') }}">Home</a>
            </li>

            {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link fs-5 fw-bold text-dark" href="{{ path('admin') }}">Dashboard</a>
                </li>
            {% endif %}
        </div>
        
        <div class="d-flex">
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <li class="nav-item">
                    <a class="nav-link fs-5 fw-bold text-dark" href="{{ path('profil') }}">Profil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link fs-5 fw-bold text-dark" href="{{ path('app_logout') }}">Logout</a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link fs-5 fw-bold text-dark" href="{{ path('register') }}">Register</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link fs-5 fw-bold text-dark" href="{{ path('login') }}">Login</a>
                </li>
            {% endif %}
        </div>
    </ul>
</nav>